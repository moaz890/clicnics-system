/** Sent on API requests and forwarded by middleware for server components. */
export const TENANT_HEADER = "X-Tenant-ID";

/** @deprecated Tenant is derived from hostname; cookie is no longer used. */
export const TENANT_COOKIE = "tenantId";
